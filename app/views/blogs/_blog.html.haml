.main
  .blogs
    .row
      .col.s12.m7
        .card
          .card-image
            %img{src: "images/sample-1.jpg"}/
            %span.card-title 
          .card-content
            %p
              = blog.body
          .card-action
            -if user_signed_in? && current_user.id == blog.user_id
              =link_to "edit", edit_blog_path(current_user) 
                
            -if user_signed_in? && current_user.id == blog.user_id
              =link_to "destroy", "/blogs/#{blog.id}", method: :delete
                